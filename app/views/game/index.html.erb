<%= content_tag "div", id: "user_details", data: {url: user_url(@user, :format => "json"), id: @user.id, email: @user.email} do %>
  <p>Logged in as: <%= @user.email %> (<%= link_to "show", @user %>)</p>
<% end %>

<h1>Geo Scorch</h1>

<div id='actions_interface'>
  <div id='attack_interface'>
    <h2>Attack</h2>

    <p>
      <label for="power">Power:</label>
      <input type="text" class="power_input" id="power" value=50 style="border:0; color:#f6931f; font-weight:bold;"/>
    </p>
    <div id='power_slider' class='power_slider'></div>
  </div>
</div>

<div id='map'></div>

